DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Contraseña compartida para entrar al sitio (login único, sin cuentas por operario).
AUTH_PASSWORD=""

# Secreto para firmar la sesión. Generar uno nuevo con: openssl rand -base64 32
AUTH_SECRET=""
